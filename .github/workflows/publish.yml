name: Publish (GitHub Packages)

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: https://npm.pkg.github.com
          scope: "@agiterra"
          always-auth: true
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build publishable packages
        run: |
          pnpm --filter @agiterra/rove-core build
          pnpm --filter @agiterra/rove-cli build
          echo '--- packages/core/dist ---'
          ls -la packages/core/dist | head -10
          echo '--- packages/cli/dist ---'
          ls -la packages/cli/dist | head -10
          echo '--- packages/cli/bin ---'
          ls -la packages/cli/bin

      - name: Publish @agiterra/rove-core
        working-directory: packages/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: pnpm publish --no-git-checks --access restricted

      - name: Publish @agiterra/rove-cli
        working-directory: packages/cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: pnpm publish --no-git-checks --access restricted

{
  "name": "@tankloop/tankloop-eval",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "tankloop-eval": "./bin/tankloop-eval.js"
  },
  "type": "module",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "@tankloop/agentic-ux-evaluator-core": "workspace:*",
    "commander": "^12.1.0",
    "marked": "^14.1.4",
    "playwright": "1.60.0-alpha-2026-05-06",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
